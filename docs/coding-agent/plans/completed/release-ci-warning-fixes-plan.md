# Release CI warning fixes

## Scope

Fix actionable Node and Flutter/Android warnings observed in GitHub Actions run `29699794676`.

Plan approval was waived: the user explicitly requested investigation and repair, and the changes were narrowly scoped to CI action versioning and Android build configuration.

## Task Waves

### Wave 1

- Task_1: Update the release workflow cache action to the Node 24-compatible major version — completed.
- Task_2: Migrate the Android app from the Kotlin Gradle Plugin to Flutter built-in Kotlin — completed.

### Wave 2

- Task_3: Integrate and validate the combined CI and Android changes — completed.

## Results

- Updated both release workflow `actions/cache` pins from v4 to the Node 24-compatible v6 SHA.
- Removed the app's direct Kotlin Gradle Plugin declarations while retaining JVM 17 compiler settings.
- Removed the committed `android.builtInKotlin` flag; Flutter may regenerate its compatibility opt-out because `integration_test` still uses legacy KGP.
- Kept informational `flutter pub get` dependency-update notices out of scope.

## Validation

- Workflow YAML parsed successfully.
- `git diff --check` passed.
- `flutter analyze --no-pub` passed.
- `flutter pub get` passed.
- `flutter build apk --debug` passed with the compatibility flag generated by Flutter.
- Reviewer independently approved the final diff.

## Progress Log

- 2026-07-20: Identified Node warnings from actions/cache v4 and Flutter warning from Kotlin Gradle Plugin usage in release run 29699794676.
- 2026-07-20: Task_1 and Task_2 completed; YAML, Flutter analyze, debug APK build, and final static checks passed. Reviewer approved the integrated diff.

## Decision Log

- 2026-07-20: Do not upgrade Flutter package dependencies solely because `flutter pub get` reports newer versions; that is an informational update notice and would expand compatibility risk beyond the CI warning fix.
- 2026-07-20: Do not commit `android.builtInKotlin=true`; the current `integration_test` dependency still applies legacy KGP and makes global built-in Kotlin fail. Leave the property absent so Flutter can generate its compatibility opt-out during builds.
